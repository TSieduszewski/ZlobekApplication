<thlogic>
    <attr sel="#tuitionId"
          th:text="${payment.tuition}"/>
    <attr sel="#mealsId"
          th:text="${payment.meals}"/>
    <attr sel="#summaryId"
          th:text="${payment.summary}"/>
    <attr sel="#monthId"
          th:text="${month}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer::footerFragment}"/>
</thlogic>